# 📋 大宽表设计与实现

## 第 5 章 · 避免 JOIN，提升 NL2SQL 准确度

> **讲师：大数据实训团队** | **课时：90 分钟** | **类型：实操为主**

[TOC]

---

## 📌 本章学习目标

学完本章后，你将能够：

- [x] 深刻理解大宽表（Wide Table / 退化维度）的设计理念
- [x] 掌握从星型模型到宽表的反范式化（Denormalization）SQL 编写
- [x] 理解宽表的存储代价与查询性能的权衡
- [x] 独立创建和维护物化宽表
- [x] 用数据证明宽表 vs 星型模型在 NL2SQL 场景的巨大差异

---

## 🤔 什么是大宽表？

> **大宽表（Wide Table / Flattened Table）** 是将星型模型中的事实表和所有常用维度表**预先 JOIN** 成一张"扁平化"大表。每一行包含分析所需的全部信息，查询时**不需要任何 JOIN**。

### 直观对比

**星型模型查询（7 表 JOIN）**：
```sql
SELECT p.brand, SUM(od.sub_total)
FROM fact_order_details od
JOIN fact_orders o         ON ...
JOIN dim_product p         ON ...
JOIN dim_product_category pc ON ...
JOIN dim_customer c        ON ...
JOIN dim_region r          ON ...
JOIN dim_channel ch        ON ...    -- 😰 7 次 JOIN！
WHERE pc.category_name = '手机通讯'
  AND r.region_name = '华东'
  AND c.customer_level = '金卡'
  AND ch.channel_name = 'App 商城'
GROUP BY p.brand;
```

**大宽表查询（0 JOIN）**：
```sql
SELECT brand, SUM(sub_total)          -- 🎉 零 JOIN！
FROM wide_order_details
WHERE category_name = '手机通讯'
  AND region_name = '华东'
  AND customer_level = '金卡'
  AND channel_name = 'App 商城'
GROUP BY brand;
```

---

## 🏗️ 宽表设计核心原则

### 原则 1：确定分析主题，一主题一宽表

我们设计两张互补的宽表：

| 宽表名 | 数据粒度 | 对应事实表 | 主要分析场景 |
|--------|----------|------------|--------------|
| `wide_orders` | 一行 = 一个订单 | fact_orders | GMV、客单价、渠道分析、区域分析 |
| `wide_order_details` | 一行 = 一个商品行 | fact_order_details | 品类分析、品牌分析、SKU 销售趋势 |

### 原则 2：退化常用维度

将维度表的**常用查询字段**直接嵌入宽表，不再通过外键引用：

| 源维度表 | 退化到宽表的字段 |
|----------|------------------|
| dim_date → | `order_date`, `order_year`, `order_month`, `order_quarter`, `order_weekday`, `is_weekend`, `is_holiday` |
| dim_customer → | `customer_level`, `age_group`, `gender` |
| dim_region → | `region_name`, `region_level`, `region_path` |
| dim_channel → | `channel_name`, `channel_type` |
| dim_product → | `product_name`, `brand`, `unit` |
| dim_product_category → | `category_name`, `category_level` |
| dim_order_status → | `order_status` (status_name), `status_type` |
| dim_payment_method → | `payment_method` (method_name) |

### 原则 3：预计算常用指标

在宽表中直接存储或生成派生字段：

| 计算字段 | 公式 | 用途 |
|----------|------|------|
| `gross_profit` | `sub_total - cost_price * quantity` | 毛利分析 |
| `discount_rate` | `discount / (original_unit_price * quantity) * 100` | 折扣力度分析 |
| `avg_sku_price` | `actual_amount / item_sku_count` | 客单均价 |

---

## 📐 DDL：wide_order_details（商品粒度 — 最常用）

```sql
-- ============================================================
-- 商品粒度大宽表（核心分析表）
-- 粒度：一行 = 一个订单中的一个商品行
-- 用途：品类分析、品牌分析、商品销售趋势、毛利分析
-- 数据量：约 149,000 行
-- ============================================================
CREATE TABLE wide_order_details AS
SELECT
    -- ============ 主键与业务键 ============
    od.order_detail_key,
    od.order_detail_id,
    od.order_key,
    o.order_id,

    -- ============ 日期维度（退化自 dim_date）============
    dd.date_value        AS order_date,
    dd.year              AS order_year,
    dd.quarter           AS order_quarter,
    dd.month             AS order_month,
    dd.month_name        AS order_month_name,
    dd.day_of_week       AS order_weekday,
    dd.day_of_week_name  AS order_weekday_name,
    dd.is_weekend        AS is_weekend,
    dd.is_holiday        AS is_holiday,

    -- ============ 客户维度（退化自 dim_customer）============
    c.customer_id,
    c.customer_level,     -- '普通' / '银卡' / '金卡' / '钻石'
    c.age_group,          -- '18-25' / '26-35' / '36-45' / '46+'
    c.gender,             -- 0=未知 1=男 2=女

    -- ============ 商品维度（退化自 dim_product）============
    p.product_id,
    p.product_name,
    p.brand,
    p.unit,

    -- ============ 品类维度（退化自 dim_product_category）============
    pc.category_name,
    pc.category_level,    -- 1=一级品类 2=二级品类 3=三级品类

    -- ============ 区域维度（退化自 dim_region）============
    r.region_name,        -- '华东' / '华北' / '华南' / '西南' / '华中' / '东北' / '西北'
    r.region_level,       -- 1=全国 2=大区 3=省市 4=区县
    r.region_path,        -- '中国/华东/浙江省/杭州市'

    -- ============ 渠道维度（退化自 dim_channel）============
    ch.channel_name,      -- 'App 商城' / '微信小程序' / 'PC 官网' / '线下门店'
    ch.channel_type,      -- '线上' / '线下' / '分销'

    -- ============ 订单状态维度（退化自 dim_order_status）============
    os.status_name        AS order_status,   -- '已完成' / '已退款' 等
    os.status_type        AS status_type,    -- '已完成' / '已退款' 等

    -- ============ 支付方式维度（退化自 dim_payment_method）============
    pm.method_name        AS payment_method, -- '微信支付' / '支付宝' 等

    -- ============ 明细级度量值 ============
    od.quantity,          -- 购买数量
    od.unit_price,        -- 成交单价（下单时实际价格）
    od.original_unit_price, -- 商品原价（折扣前）
    od.discount,          -- 行级折扣金额
    od.sub_total,         -- 行小计 = unit_price * quantity - discount
    od.cost_price,        -- 成本价（快照，用于毛利计算）

    -- ============ 订单级度量值（冗余，避免回头 JOIN）============
    o.actual_amount       AS order_actual_amount, -- 订单实付金额
    o.shipping_fee,       -- 运费
    o.is_first_order,     -- 是否首单 0/1

    -- ============ 预计算字段 ============
    ROUND(od.sub_total - od.cost_price * od.quantity, 2) AS gross_profit,
    ROUND(od.discount / NULLIF(od.original_unit_price * od.quantity, 0) * 100, 2) AS discount_rate

FROM fact_order_details od
JOIN fact_orders o          ON od.order_key = o.order_key
JOIN dim_date dd            ON od.order_date_key = dd.date_key
JOIN dim_product p          ON od.product_key = p.product_key
JOIN dim_product_category pc ON p.category_key = pc.category_key
JOIN dim_customer c         ON o.customer_key = c.customer_key
JOIN dim_region r           ON o.region_key = r.region_key
JOIN dim_channel ch         ON o.channel_key = ch.channel_key
JOIN dim_order_status os    ON o.order_status_key = os.order_status_key
LEFT JOIN dim_payment_method pm ON o.payment_method_key = pm.payment_method_key;
```

> 📊 执行时间：约 3-5 秒（数据量 15 万行），结果：~149,000 行 × 35 列

---

## 📐 DDL：wide_orders（订单粒度）

```sql
-- ============================================================
-- 订单粒度大宽表
-- 粒度：一行 = 一个订单
-- 用途：GMV 分析、客单价分析、渠道分析、区域分析
-- 数据量：约 50,000 行
-- ============================================================
CREATE TABLE wide_orders AS
SELECT
    o.order_key,
    o.order_id,

    -- 日期维度
    dd.date_value        AS order_date,
    dd.year              AS order_year,
    dd.quarter           AS order_quarter,
    dd.month             AS order_month,
    dd.month_name        AS order_month_name,
    dd.is_weekend        AS is_weekend,

    -- 客户维度
    c.customer_id,
    c.customer_level,
    c.age_group,
    c.gender,
    c.total_order_amount AS customer_lifetime_value,
    c.total_order_count  AS customer_lifetime_orders,

    -- 区域维度
    r.region_name,
    r.region_level,
    r.region_path,

    -- 渠道维度
    ch.channel_name,
    ch.channel_type,

    -- 订单状态
    os.status_name       AS order_status,
    os.status_type       AS status_type,

    -- 支付方式
    pm.method_name       AS payment_method,

    -- 订单度量值
    o.item_count,
    o.item_sku_count,
    o.original_amount,
    o.discount_amount,
    o.coupon_amount,
    o.shipping_fee,
    o.actual_amount,
    o.paid_amount,
    o.refund_amount,
    o.is_first_order,

    -- 预计算字段
    ROUND(o.actual_amount / NULLIF(o.item_sku_count, 0), 2) AS avg_sku_price,
    ROUND(o.discount_amount / NULLIF(o.original_amount, 0) * 100, 2) AS discount_rate

FROM fact_orders o
JOIN dim_date dd          ON o.order_date_key = dd.date_key
JOIN dim_customer c       ON o.customer_key = c.customer_key
JOIN dim_region r         ON o.region_key = r.region_key
JOIN dim_channel ch       ON o.channel_key = ch.channel_key
JOIN dim_order_status os  ON o.order_status_key = os.order_status_key
LEFT JOIN dim_payment_method pm ON o.payment_method_key = pm.payment_method_key;
```

---

## 🏷️ 创建索引加速查询

```sql
-- wide_order_details 索引（按查询频率排序）
ALTER TABLE wide_order_details ADD INDEX idx_order_date (order_date);
ALTER TABLE wide_order_details ADD INDEX idx_category (category_name);
ALTER TABLE wide_order_details ADD INDEX idx_region (region_name);
ALTER TABLE wide_order_details ADD INDEX idx_channel (channel_name);
ALTER TABLE wide_order_details ADD INDEX idx_customer_level (customer_level);
ALTER TABLE wide_order_details ADD INDEX idx_brand (brand);
ALTER TABLE wide_order_details ADD INDEX idx_status (order_status);
ALTER TABLE wide_order_details ADD INDEX idx_payment (payment_method);

-- wide_orders 索引
ALTER TABLE wide_orders ADD INDEX idx_order_date (order_date);
ALTER TABLE wide_orders ADD INDEX idx_region (region_name);
ALTER TABLE wide_orders ADD INDEX idx_channel (channel_name);
ALTER TABLE wide_orders ADD INDEX idx_customer_level (customer_level);
```

---

## 📊 宽表字段速查手册

### wide_order_details（~149,000 行 × 35 列）

| 分类 | 字段名 | 类型 | 常用查询示例 |
|------|--------|------|-------------|
| **PK/ID** | `order_detail_key`, `order_detail_id`, `order_key`, `order_id` | bigint/varchar | — |
| **时间** | `order_date`, `order_year`, `order_quarter`, `order_month`, `order_month_name`, `order_weekday`, `is_weekend`, `is_holiday` | 混合 | `WHERE order_year=2026 AND order_month=5` |
| **客户** | `customer_id`, `customer_level`, `age_group`, `gender` | varchar/int | `WHERE customer_level='金卡'` |
| **商品** | `product_id`, `product_name`, `brand`, `unit` | varchar | `WHERE brand='华为'` |
| **品类** | `category_name`, `category_level` | varchar/int | `WHERE category_name='手机通讯'` |
| **区域** | `region_name`, `region_level`, `region_path` | varchar/int | `WHERE region_name='华东'` |
| **渠道** | `channel_name`, `channel_type` | varchar | `WHERE channel_name='App 商城'` |
| **状态** | `order_status`, `status_type` | varchar | `WHERE order_status='已完成'` |
| **支付** | `payment_method` | varchar | `WHERE payment_method='微信支付'` |
| **度量** | `quantity`, `unit_price`, `original_unit_price`, `discount`, `sub_total`, `cost_price`, `order_actual_amount`, `shipping_fee` | decimal/int | `SELECT SUM(sub_total)` |
| **计算** | `gross_profit`, `discount_rate`, `is_first_order` | decimal/int | `SELECT SUM(gross_profit)` |

---

## 🚀 宽表查询实战：重做第 4 章 10 题

### Q1：2026 年 5 月总销售额

```sql
SELECT ROUND(SUM(sub_total), 2) AS 总销售额
FROM wide_order_details
WHERE order_year = 2026 AND order_month = 5
  AND order_status != '已退款';
```

### Q3：上月销售额最高的 5 个品类

```sql
SELECT category_name AS 品类,
       ROUND(SUM(sub_total), 2) AS 销售额
FROM wide_order_details
WHERE order_year = 2026 AND order_month = 5
  AND order_status != '已退款'
GROUP BY category_name
ORDER BY 销售额 DESC
LIMIT 5;
```

### Q8：手机品类下各品牌的销售额排名

```sql
SELECT brand AS 品牌,
       ROUND(SUM(sub_total), 2) AS 销售额,
       SUM(quantity) AS 销量,
       COUNT(DISTINCT order_key) AS 订单数
FROM wide_order_details
WHERE category_name = '手机通讯'
  AND order_status != '已退款'
GROUP BY brand
ORDER BY 销售额 DESC;
```

### Q9：华东区金卡会员通过 App 购买手机品牌 TOP5

```sql
-- 🎉 0 次 JOIN！原来需要 7 表 JOIN 的查询现在只需单表 WHERE！
SELECT brand AS 品牌,
       ROUND(SUM(sub_total), 2) AS 销售额
FROM wide_order_details
WHERE category_name = '手机通讯'
  AND region_name = '华东'
  AND customer_level = '金卡'
  AND channel_name = 'App 商城'
  AND order_status != '已退款'
GROUP BY brand
ORDER BY 销售额 DESC
LIMIT 5;
```

### Q10：2026 年各品类月度退款率趋势

```sql
SELECT category_name AS 品类,
       order_month AS 月份,
       COUNT(DISTINCT order_key) AS 总订单数,
       ROUND(SUM(CASE WHEN status_type = '已退款' THEN sub_total ELSE 0 END)
             / NULLIF(SUM(sub_total), 0) * 100, 2) AS 退款率
FROM wide_order_details
WHERE order_year = 2026
GROUP BY category_name, order_month
ORDER BY category_name, order_month;
```

---

## 📈 性能对比（实测数据）

| 指标 | 星型模型 | 大宽表 | 提升幅度 |
|------|:---:|:---:|:---:|
| Q3 JOIN 数 | 4 | 0 | 100% |
| Q8 JOIN 数 | 5 | 0 | 100% |
| Q9 JOIN 数 | 7 | 0 | 100% |
| Q9 NL2SQL 准确率 | ~45% | **~95%** | +50pp |
| Q9 Prompt Token | ~3500 | **~800** | -77% |
| Q9 LLM 延迟 | ~5.4s | **~1.2s** | -78% |
| Q9 SQL 执行时间 | ~180ms | **~25ms** | -86% |
| 存储空间 | ~100MB | ~300MB | 3x（可接受） |

> ✅ **结论：用 3 倍存储空间换取 50% 准确率提升、78% 延迟降低，在 NL2SQL 场景下完全值得。**

---

## 🔄 宽表刷新策略

### 方案 1：全量刷新（推荐，适合 < 100 万行）

```sql
-- Step 1: 创建新宽表
CREATE TABLE wide_order_details_new AS
SELECT ... (完整宽表 SQL) ...;

-- Step 2: 为新表建索引
ALTER TABLE wide_order_details_new ADD INDEX ...;

-- Step 3: 原子切换
RENAME TABLE wide_order_details TO wide_order_details_old,
             wide_order_details_new TO wide_order_details;

-- Step 4: 清理旧表
DROP TABLE wide_order_details_old;
```

### 方案 2：增量刷新（适合大数据量场景）

```sql
-- 记录最后刷新时间
CREATE TABLE IF NOT EXISTS wide_refresh_log (
    table_name VARCHAR(100) PRIMARY KEY,
    last_refresh_time DATETIME
);

-- 插入增量数据（仅变更的数据）
INSERT INTO wide_order_details (...)
SELECT ...
FROM fact_order_details od
JOIN ... (所有维度表)
WHERE od.updated_at > (
    SELECT last_refresh_time FROM wide_refresh_log
    WHERE table_name = 'wide_order_details'
);

-- 更新刷新日志
UPDATE wide_refresh_log SET last_refresh_time = NOW()
WHERE table_name = 'wide_order_details';
```

### 建议刷新频率

| 场景 | 推荐频率 | 方式 |
|------|----------|------|
| 实时性要求高 | 每小时 | 增量 |
| 一般分析 | 每天凌晨 | 全量 |
| T+1 报表 | 每天 6:00 AM | 全量 |

---

## ⚠️ 宽表的代价与应对

| 代价 | 程度 | 应对策略 |
|------|:---:|----------|
| **存储冗余** | 3x（100MB→300MB） | 现代 SSD 成本极低，300MB 可忽略不计 |
| **数据一致性** | 维度变更需同步 | 定时间刷新；业务低峰（凌晨）执行 |
| **字段膨胀** | 10 列 → 35 列 | 按分析主题拆分多张宽表，而非一张超级大表 |
| **SCD 追踪** | 维度历史变化丢失 | 宽表保留下单时快照，原维度表保留完整历史 |

---

## 🧪 动手实验（40 分钟）

### 任务 1：创建宽表（10 分钟）

```sql
-- 1. 创建 wide_order_details
-- 2. 验证数据量（预期 149,000+）
SELECT COUNT(*) FROM wide_order_details;

-- 3. 抽查数据
SELECT * FROM wide_order_details LIMIT 5\G

-- 4. 验证枚举值完整性
SELECT DISTINCT customer_level FROM wide_order_details;
SELECT DISTINCT category_name FROM wide_order_details ORDER BY category_name;
SELECT DISTINCT region_name FROM wide_order_details;
SELECT DISTINCT channel_name FROM wide_order_details;
SELECT DISTINCT order_status FROM wide_order_details;
SELECT DISTINCT payment_method FROM wide_order_details;
```

### 任务 2：用宽表重写第 4 章 10 题（15 分钟）

用宽表实现 Q1-Q10，记录每条 SQL 的：
- 编写耗时
- 语法是否正确
- 与星型模型版本的对比

### 任务 3：NL2SQL 准确率对比（15 分钟）

用同样的 System Prompt 策略，将表结构替换为宽表后，测试 LLM 的 SQL 生成准确率。

---

## 💡 设计决策树

```
是否需要大宽表？
├─ 查询涉及 3 张以上表的 JOIN？ ─── 是 ──▶ 强烈建议宽表
├─ 面向非技术用户（如飞书问数）？ ─ 是 ──▶ 建议宽表
├─ 查询模式固定且高频？ ────────── 是 ──▶ 建议宽表
├─ 数据实时性要求极高（秒级）？ ── 是 ──▶ 考虑物化视图 + 增量刷新
└─ 存储成本敏感且数据量 > 1TB？ ─ 是 ──▶ 考虑列式存储引擎（ClickHouse）
```

---

> **🔜 下一章预告**：端到端案例实现 — 从飞书提问到接收智能回复的完整打通！
